<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ucla.fusa.android.actividades" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Fusa_theme">
        <activity
            android:name=".VistasInicialesActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VistasPrincipalesActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".VistasAccesoActivity"
            android:label="@string/titulo_action_bar_login">

        </activity>
    </application>

</manifest>
